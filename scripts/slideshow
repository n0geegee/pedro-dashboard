#!/usr/bin/env bash
# Pedro Dashboard — short operator entrypoint: scripts/slideshow on|off|status
set -uo pipefail
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec python3 "$SCRIPT_DIR/pedro-display-switch.py" "$@"
