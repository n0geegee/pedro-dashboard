#!/usr/bin/env bash
# Pedro Dashboard — canonical operator command: pedro-display-switch on|off|status|restart-cycle
set -uo pipefail
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec python3 "$SCRIPT_DIR/pedro-display-switch.py" "$@"
