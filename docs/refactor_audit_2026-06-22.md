# Pedro Dashboard refactor audit — 2026-06-22

> Refactor pack: `pedro_dashboard_refactor_pack_CORRECTED_2026-06-22.zip`
> Verified SHA256SUMS inside pack on 2026-06-22 by `sha256sum -c` (10/10 OK).
> Generated by MiniMax-M3 (Pedro iMac-Hermes).

## Root

- Project root: `/home/imac-hermes/projects/pedro_dashboard`
- Git remote: `https://github.com/n0geegee/pedro-dashboard.git`
- Local version file: `VERSION = 1.4.11`
- Active runtime URL: `http://127.0.0.1:17888/`
- Runtime port source of truth: `scripts/_lifecycle_common.sh` (`PEDRO_PORT=17888`)

## Git state at audit (Phase 0)

```
## main...origin/main [do przodu 2]
 M scripts/start-kiosk.sh
 M scripts/watchdog-dashboard.sh
```

Dirty files purpose (verified from `git diff`):
- `scripts/start-kiosk.sh` — wait up to `PEDRO_KIOSK_HEALTH_WAIT_SECONDS` (default 30s) for backend health before launching kiosk. Fix for XFCE autostart race between server and kiosk entries.
- `scripts/watchdog-dashboard.sh` — if backend health is OK but kiosk process is missing and display works, watchdog now starts the kiosk.

Both are protected; Phase 0 backup saved them.

Last 5 commits:
- `854249c v1.4.11 accept LR full-frame slideshow`
- `0998792 v1.4.10 randomize slideshow queue`
- `ce366ab v1.4.9 always-listening KWS voice daemon (Vosk + Gemini + espeak)`
- `37a8182 v1.4.8 photos: native fetch + monotonic advance + orientation + 5s rotator`
- `27243dc v1.4.7 vnl match times in correct UTC instant (TZ fix)`

Git identity configured: `iMac-Hermes <pedro@iMac-Hermes.local>`.

## Phase 0 backup artifacts

```
/home/imac-hermes/projects/pedro_dashboard_backups/
├── refactor-preflight-20260622-123334.git-status.txt   (95 bytes)
├── refactor-preflight-20260622-123334.dirty.patch      (54 lines, 2 scripts)
├── pedro_dashboard_pre_refactor_20260622-123334.tgz    (31 MB, sha256 below)
└── pedro_dashboard_pre_refactor_20260622-123334.tgz.sha256
```

```
5c5d1458dfd6f714eea9d6e754b86b8ba90950d6496a12aa802f0dd9fce58e85  pedro_dashboard_pre_refactor_20260622-123334.tgz
```

Tar warning during backup (`./app/state: plik zmienił się w trakcie czytania`): `state-refresher.sh --loop --interval 20` writes new JSONs every 20s. One widget JSON may be mid-write in the snapshot, but the live state is rebuilt continuously; archive is safe.

## Live processes (verified)

| Process | PID | Etime | RSS | Role |
|---|---|---|---|---|
| `python3 app/server.py` | 3865 | 03:26:08 | 26324 KB | Backend on :17888 |
| `chrome --kiosk :17888` | 4707 | 03:22:51 | 225708 KB | Kiosk browser |
| `state-refresher.sh --loop --interval 20` | 2840 | 03:26:17 | 3568 KB | Refreshes `app/state/*.json` |
| `watchdog-dashboard.sh --loop --interval 30` | 4508 | 03:23:22 | 3732 KB | Kiosk/server health watchdog |
| `pedro_voice_kws.py` (venv) | 2862 | 03:26:17 | 233456 KB | Vosk KWS always-listening |
| `kiosk-brightness-loop.py` | 2799 | 03:26:18 | 12212 KB | Panel brightness loop |

All processes started ~03:22–03:26 ago (≈ after morning-kiosk-refresh at 06:41 or earlier).

## Active endpoints (smoke results)

### `GET /api/health` (200 OK)

```json
{
  "status": "ok",
  "service": "pedro_dashboard",
  "version": "0.1.0",
  "etap": 1,
  "updated_at": "2026-06-22T10:31:45+00:00",
  "uptime_seconds": 12231,
  "host": "127.0.0.1",
  "port": 17888,
  "privacy_mode": "private"
}
```

**Mismatch noted (good small refactor target):** `VERSION` file says `1.4.11`, but `/api/health.version` is hardcoded `0.1.0`. Picked-up as Phase 3 task #2.

### `GET /api/state` top-level keys

```
['poland_match_today', 'privacy_mode', 'server', 'status', 'updated_at', 'widgets']
```

- `server = {started_at, uptime_seconds, host, port}` (port = 17888)
- `privacy_mode = "private"`
- `widgets` keys (15): `alerts, calendar, current_focus, dashboard, decisions, hermes, ll_tbd, media, openviking, route, skin, system, voice_console, volleyball, weather`

Note: `skin_override.json` is on disk but NOT exposed in `widgets` top-level (likely reserved/dormant).

### `GET /api/voice_console` top-level keys

```
['_widget', 'activity', 'error', 'privacy_mode', 'result', 'status', 'ttl_seconds', 'updated_at', 'utterance', 'voice']
```

All five required fields (`voice`, `utterance`, `activity`, `result`, `error`) present. Privacy mode = `private`. Status = `ok`.

### `GET /api/minimax_usage`

```json
{"status": "ok", "updated_at": 1782110836, "data": {"five_h_in": 1350347, ...}}
```

Confirms MiniMax 5h quota widget already wired and producing live data (the "MiniMax token estimator" task referenced in MEMORY appears to be already shipped as this endpoint + UI card).

## State file inventory (`app/state/`)

17 files, 16 used by `/api/state.widgets` (skin_override is dormant):

| File | Last write | Size | Owner (connector) |
|---|---|---|---|
| alerts.json | 12:33 | 547 B | local probe |
| calendar.json | 12:33 | 1058 B | Kamila Calendar |
| current_focus.json | 12:33 | 669 B | local probe |
| dashboard.json | 12:33 | 604 B | local probe |
| decisions.json | 12:33 | 1294 B | local probe |
| hermes.json | 12:33 | 663 B | Hermes status |
| ll_tbd.json | 12:33 | 275 B | placeholder |
| media.json | 12:33 | 1498 B | Polsat + Photos |
| openviking.json | 12:33 | 612 B | OV status |
| photos_manifest.json | 12:33 | 127 KB | Google Photos cache |
| route.json | 12:33 | 640 B | Google Routes ETA |
| skin.json | 12:33 | 582 B | seasonal skin |
| skin_override.json | 06-16 | 195 B | dormant |
| system.json | 12:33 | 992 B | local probe |
| voice_console.json | 12:34 | 737 B | voice KWS/runner |
| volleyball.json | 12:33 | 9.5 KB | match calendar |
| weather.json | 12:33 | 1250 B | Open-Meteo |

All refresh at 20 s cadence via `state-refresher.sh`. skin_override not refreshed (not in rotation).

## Scripts inventory (relevant)

Lifecycle (must preserve):
- `_lifecycle_common.sh` — env defaults (PEDRO_PORT=17888, PEDRO_STATE_DIR, PEDRO_LOG_DIR, PEDRO_RUN_DIR, PEDRO_CHROME_PROFILE_DIR)
- `start-dashboard.sh`, `stop-dashboard.sh`, `status-dashboard.sh`
- `start-kiosk.sh` (dirty)
- `watchdog-dashboard.sh` (dirty)
- `state-refresher.sh`, `refresh-all-state.sh`
- `install-autostart.sh`

Connectors (must preserve):
- `refresh-kamila-calendar.py`, `refresh-match-calendar.py`, `refresh-route-status.py`, `refresh-weather.py` (likely), `refresh-polsat-status.py`, `refresh-photos-slideshow.py`, `refresh-hermes-status.py`, `refresh-openviking-status.py`

Voice (must preserve):
- `pedro_voice_kws.py`, `pedro_voice_daemon.py`, `pedro-voice-record.py`, `pedro-voice-stt.py`, `pedro-voice-tts.py`, `pedro-runner.py`, `mock-voice-result.py`

Other:
- `kiosk-brightness-loop.{py,sh}` — display brightness cycle
- `photos-rotator.sh` — slideshow advance daemon
- `hide-xfce-panel-for-kiosk.sh`, `launch-polsat-box-go.sh` — kiosk UX

## Untouchable items (visual/product lock)

- v1.4.11 LR Google Photos slideshow full-frame geometry (commit `854249c`)
- 1920×1200 no-scroll room layout
- LL = Pedro Voice Console
- Passive kiosk (no buttons / links / click handlers)
- Bottom static match/results strip
- Polsat overlay geometry
- Seasonal skin mechanism
- Port 17888 (PEDRO_PORT env override semantics)
- `/api/state` `state.widgets` envelope (frontend `app/static/app.js` reads it)
- `/api/voice_console` top-level `voice/utterance/activity/result` contract (NOT collapsed into `data`)
- MX Linux / no-systemd (no `systemctl` requirement)
- Live connectors (calendar/weather/route/volleyball/media/photos/voice/system/hermes/openviking)

## Open small refactor candidates (Phase 3 priority order)

1. Centralize runtime constants without behavior change (e.g. `app/config.py` reading VERSION and PEDRO_PORT).
2. Read `VERSION` file (1.4.11) for `/api/health.version` instead of hardcoded `0.1.0`.
3. Add stdlib `unittest` for `/api/state` shape, voice privacy modes, stale/empty/error handling.
4. Optional module extraction (`app/state_store.py`, `app/redaction.py`) — only after items 1–3 are green.
5. Cleanup `app/server.py` historical `17890` default to align with `PEDRO_PORT` env override.

## Verification commands run in this audit

```bash
pwd                                                 # /home/imac-hermes/projects/pedro_dashboard
git status --short --branch
git remote -v
git diff --stat
curl -fsS --max-time 5 http://127.0.0.1:17888/api/health
curl -fsS --max-time 5 http://127.0.0.1:17888/api/state
curl -fsS --max-time 5 http://127.0.0.1:17888/api/voice_console
curl -fsS --max-time 5 http://127.0.0.1:17888/api/minimax_usage
python3 -m py_compile app/server.py scripts/*.py      # rc=0
ps -eo pid,etime,rss,cmd | grep -E 'pedro_dashboard|start-kiosk|watchdog-dashboard|state-refresher|google-chrome --kiosk|pedro_voice_kws'
sha256sum -c SHA256SUMS.txt                          # 10/10 OK in refactor pack
```

All green. Kiosk is healthy, all endpoints respond, no blocked process, no compile errors.

## Blocker status

- No blockers.
- Backup done (Phase 0).
- Audit doc committed next (Phase 1).
- Phase 2 (branch) after Mastermind/Jurand acknowledges audit.
- Phase 3+ not started yet.