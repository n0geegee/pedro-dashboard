# Pedro seasonal GPT2 overlay drafts — 2026-06-15

Generated through Codex built-in `$imagegen`, not FAL and not OpenClaw.

Raw GPT2 outputs:
- `02_RAW_GPT2/*.png` — exact generated files, 1586×992 PNG.

Dashboard-ready derivatives:
- `05_WEB_OVERLAYS_1920x1200/*.webp` — resized only to Pedro 1920×1200, no redraw/overlay reconstruction.

Current recommendation:
1. Use `spring_fairytale` as the first expressive skin test.
2. `winter_frost` is the cleanest/readability safest.
3. `summer_glow` is strong but slightly brighter in upper-right; test under Polsat/UR.
4. `autumn_amber` is usable, calm, and least risky, but less distinctive.

Do not activate these live until Jurand approves a direction. To test, copy a `.webp` into `app/static/skins/` and point the relevant `--skin-overlay-url` at it.
